{
  "name": "@burnt-finance/demo",
  "version": "0.1.0",
  "private": true,
  "cacheDirectories": [
    "client/node_modules",
    "server/node_modules"
  ],
  "scripts": {
    "client:build": "bash package.sh client run build",
    "client:install": "bash package.sh client install",
    "server:build": "bash package.sh server run build",
    "server:start": "bash package.sh server run start",
    "server:install": "bash package.sh server install",
    "start": "npm run server:start",
    "build": "bash package.sh both run build",
    "lint": "bash package.sh both run lint",
    "lint:fix": "bash package.sh both run lint:fix",
    "format": "bash package.sh both run format",
    "format:fix": "bash package.sh both run format:fix",
    "postinstall": "bash package.sh both install"
  }
}
