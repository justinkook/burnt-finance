{
  "name": "burnt-finance",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm start --prefix server && npm start --prefix client",
    "dev": "npm run start:dev --prefix server && npm start --prefix client"
  }
}
